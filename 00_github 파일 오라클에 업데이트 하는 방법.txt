📋 오라클 서버 업데이트 메모장 (GitHub 연동용)
📌 [전제 조건]
PC에서 코드를 수정한 뒤 GitHub(jbc0117/asset)에 파일 업로드가 완료된 상태에서 진행합니다.


Step 1. PC에서 오라클 서버 접속하기
키보드의 Win + R 누른 후 cmd 입력하여 명령 프롬프트 열기

아래 명령어를 복사하여 실행 (asset 폴더로 이동 후 SSH 접속)

cd "C:\Users\장병찬\Desktop\asset"
ssh -i ssh-key-2026-08-14.key ubuntu@131.186.59.87



Step 2. 서버에서 GitHub 최신 코드 받아오기
서버 터미널(ubuntu@my-asset:~$)에 접속되면 아래 명령어 3개를 순서대로 실행합니다.

# 1. 앱 폴더로 이동
cd ~/asset

# 2. GitHub 최신 파일로 강제 덮어쓰기
git fetch --all && git reset --hard origin/main

# 3. index.html을 templates 폴더로 이동
mkdir -p templates && mv index.html templates/ 2>/dev/null || true



Step 3. Flask 앱 재시작하기 (24시간 백그라운드 적용)
수정된 코드가 화면에 반영되도록 앱을 다시 켭니다. (PC를 꺼도 24시간 작동 유지)

# 1. 기존 실행 중인 앱 종료
pkill -f app.py

# 2. 최신 앱을 백그라운드로 실행
nohup python3 app.py > app.log 2>&1 &



Step 4. 휴대폰 / 태블릿 접속 확인
스마트폰 브라우저 주소창에 아래 주소를 입력하여 확인합니다.

접속 주소: [http://131.186.59.87:8501](http://131.186.59.87:8501)

💡 Tip: 접속했는데 화면이 바뀌지 않는다면 휴대폰 브라우저의 새로고침 또는 방문 기록/캐시 삭제를 해주시면 즉시 반영됩니다.